<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhijit.mobiletest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BTActivity" />
        <activity android:name=".WIFIActivity" />
        <activity android:name=".SYSActivity" />
        <activity android:name=".DataSpeedActivity" />
        <activity android:name=".UIActivity" />
        <activity android:name=".SensorActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".MISCActivity" />
        <activity android:name=".CallTest" />
        <activity android:name=".Messagetest" />
        <activity android:name=".CameraTest" />
        <activity android:name=".FileTest"></activity>
    </application>

</manifest>